# Save Configuration - Cloud Deployment

[server]
bind_address = "0.0.0.0:9000"
max_body_size = 104857600  # 100 MB
worker_threads = 4         # Match CPU cores
max_blocking_threads = 512
bucket_cache_ttl_secs = 60

[storage]
data_path = "/var/lib/save/data"
metadata_path = "/var/lib/save/metadata"
max_object_size = 5368709120
gc_interval_secs = 600
gc_temp_file_max_age_secs = 3600
fsync_mode = "data"  # Options: "full", "data", "none"

[metadata]
write_buffer_size_mb = 128
max_write_buffer_number = 4
block_cache_size_mb = 512   # Increased for better read performance
max_background_jobs = 4

[credentials]
access_key = "test-access-key"
secret_key = "test-secret-key"

[limits]
max_concurrent_requests = 1000
requests_per_second = 500  # Increased for better throughput
request_timeout_secs = 300

[shutdown]
drain_timeout_secs = 30
